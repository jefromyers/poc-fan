# Copy to .env and fill in your key, then: docker compose up --build
# The key is used server-side only and is never exposed to the browser.
OPENAI_API_KEY=sk-...

# Used by docker-compose; override only if you change the db service.
# DATABASE_URL=postgres://app:app@db:5432/app
